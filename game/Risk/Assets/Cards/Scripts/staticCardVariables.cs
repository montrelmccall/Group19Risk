using UnityEngine;
using System.Collections;

public static class staticCardVariables{

    /*
      0 can choose a card
      1 a card is chosen, can manipulate it
      2 zoomed in
    */
    public static int cardMode = 0;
    public static bool active = false;
}
